// Zero-config tooling ideas: HMR, TypeScript checks, code splitting.
// Explanation: Modern tools handle these without manual setup.

const features = ["HMR", "Type checking", "Code splitting", "Tree shaking"];
console.log(features.join(", "));
// Output:
// HMR, Type checking, Code splitting, Tree shaking
